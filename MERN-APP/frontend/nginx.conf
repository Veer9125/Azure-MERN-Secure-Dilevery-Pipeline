server {
    # switching back to port 80 for simpliocity, as using non-privileged ports adds complexity
    listen 80;
    server_name _;
    
    # Frontend static files
    root /usr/share/nginx/html;
    index index.html;
    
    # Gzip compression for better performance
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    
    # Frontend routes - SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # API proxy - This is the key part!
    location /api/ {
        # I removed /api prefix and forward to backend
        rewrite ^/api/(.*)$ /$1 break;
        
        # Proxy to backend service
        proxy_pass http://backend-service.mern-app.svc.cluster.local:5050;
        
        # Proxy headers
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
    
    # Health check endpoint
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}