# A simple MERN stack application

**Note** - To run this project using `docker compose`, follow the below steps.

Switch to the `compose` branch to learn the

1. Implementation of `Dockerfile` for `client` and `server`.
2. Run the containers using `Docker Compose`.

## Run it local without Docker

### Prerequisite

- Install `npm`

#### Start backend Server:

```
cd mern/backend
npm install
npm start
```

#### Start frontend Client

```
cd mern/frontend
npm install
npm run dev
```

**Running Instance Screenshot**

<img width="1790" alt="Screenshot 14/12/2025 at 1:30 AM" src="./MERN-APP/screenshots/Screenshot-1.png">
